server:
  port: 9001

spring:
  application:
    name: yx-eureka-server
  profiles:
    active: prod

security:
  basic:
    enabled: true # 启用身份认证
  user:
    name: smile # 定义用户名
    password: 123456 # 定义密码
