version: '2'
services:
  yx-eureka:
    build:
      context: ./
      dockerfile: Dockerfile-eureka
    restart: always
    ports:
    - 9001:9001

  yx-config:
    links:
    - yx-eureka:eureka
    build:
      context: ./
      dockerfile: Dockerfile-config
    restart: always
